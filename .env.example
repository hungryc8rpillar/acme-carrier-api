FMCSA_API_KEY=your-fmcsa-key
API_KEY=generate-with-python-secrets-token_urlsafe-32
CORS_ALLOWED_ORIGINS=*
ENABLE_DOCS=true
# Comma-separated MC numbers (no MC prefix) to force-flag as ineligible for the
# Loom demo. Real FMCSA returns these as active, but the override returns
# eligible:false with reason=operating_authority_inactive. Leave blank in prod.
DEMO_INELIGIBLE_MCS=
